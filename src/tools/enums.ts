enum TranslatorType {
  Google,
  Youdao,
  Baidu
}

enum FrameMode {
  Contrast,
  Focus
}

enum YoudaoStatus {
  Success,
  Fail
}

enum MessageType {
  WindowOpt,
  TranslateResult,
  OpenMenu,
  Router
}

enum WinOpt {
  Minify,
  Drag,
  Resize,
  Zoom
}

export { TranslatorType, FrameMode, YoudaoStatus, MessageType, WinOpt };
