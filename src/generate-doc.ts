import {
  normalActionTypes,
  constantActionTypes,
  switchActionTypes,
  roles
} from "./tools/types";

import { initConfig } from "./tools/configuration";
let config = initConfig();

console.log(normalActionTypes);
